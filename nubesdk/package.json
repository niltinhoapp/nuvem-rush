{
  "name": "nuvem-rush-nubesdk",
  "version": "0.1.0",
  "private": true,
  "description": "Modulo NubeSDK (storefront/checkout) do Nuvem Rush — sinal de carrinho abandonado.",
  "type": "module",
  "scripts": {
    "dev": "tsup --watch --onSuccess \"node scripts/dev-server.mjs\"",
    "build": "tsup",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tiendanube/nube-sdk-types": "^0.5.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.6.0"
  }
}
